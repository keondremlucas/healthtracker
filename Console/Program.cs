using System;
using ClassLib;
namespace Console
{
    class Program
    {
        static void Main(string[] args)
        {
            bool program = true;
            while(program = true)
            {
                Console.WriteLine("Welcome to the Health Tracker! What would you like to do?");
                // Console.WriteLine("1. Add Record 2. Update Record 3. Delete Record");
                // var selection = Console.ReadLine();
                // if(selection = "1")
                // {
                //     Console.WriteLine("Enter in a record...");
                //     var input = console.ReadLine();
                //     input
                // }
            }
            
        }
    }
}
